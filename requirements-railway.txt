# =============================================================================
# Tektronix MCP Server — Railway / Cloud Deployment Requirements
# Python 3.11+ recommended
# =============================================================================
# This is the lean requirements file for the hosted server on Railway.
# It includes only what the MCP server itself needs to run.
# Excludes: PTA GUI (tkinter/reportlab/matplotlib), voice control (PyAudio etc),
#           PyVISA (instrument control needs a local VISA connection anyway).
# =============================================================================

# ── MCP Server core ───────────────────────────────────────────────────────────
mcp>=1.0.0
fastmcp>=3.0.0
pydantic>=2.0.0
anyio>=4.0.0
httpx>=0.27.0

# ── HTTP server (needed for streamable-http transport) ────────────────────────
uvicorn>=0.29.0
starlette>=0.37.0
